kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: {{ .Values.appName }}
  name: {{ .Values.appName }}
spec:
  ports:
  - port: {{ .Values.service.nextcloudPort }}
    targetPort: 80
  - port: {{ .Values.service.mariadbPort }}
    targetPort: 3306
  selector:
    k8s-app: {{ .Values.appName }}
